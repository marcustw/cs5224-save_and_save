import dashboard from './dashboard';
import search from './search';
import inventory from './inventory';
import analytic from './analytic';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, search, inventory, analytic]
};

export default menuItems;
